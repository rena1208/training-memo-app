[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.115.12",
    "prisma>=0.15.0",
]

[dependency-groups]
dev = [
    "ruff>=0.11.7",
]
